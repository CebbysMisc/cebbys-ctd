[project]
name = "cebbys-ctd-loader"
version = "0.1.0"
description = "Main loader for CTD language"
requires-python = ">=3.13"
dependencies = [
    "cebbys-ctd-resolver",
    "cebbys-ctd-meta",
    "cebbys-ctd-types",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sources/lv"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.uv.sources]
cebbys-ctd-resolver = { path = "../cebbys-ctd-resolver" }
cebbys-ctd-meta = { path = "../cebbys-ctd-meta" }
cebbys-ctd-types = { path = "../cebbys-ctd-types" }

[tool.pytest.ini_options]
pythonpath = ["sources"]

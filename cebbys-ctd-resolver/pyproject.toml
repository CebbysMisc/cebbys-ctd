[project]
name = "cebbys-ctd-resolver"
version = "0.1.0"
description = "Type resolution for CTD language"
requires-python = ">=3.13"
dependencies = [
    "cebbys-ctd-meta",
    "cebbys-ctd-types",
    "cebbys-ctd-utility",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sources/lv"]

[tool.uv.sources]
cebbys-ctd-meta = { workspace = true }
cebbys-ctd-types = { workspace = true }
cebbys-ctd-utility = { workspace = true }

[tool.pytest.ini_options]
pythonpath = ["sources", "tests", ".."]

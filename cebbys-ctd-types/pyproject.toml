[project]
name = "cebbys-ctd-types"
version = "0.1.0"
description = "Type definitions for CTD language"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sources/lv"]

[tool.pytest.ini_options]
pythonpath = ["sources"]
